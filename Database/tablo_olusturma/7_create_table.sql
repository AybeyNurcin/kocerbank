CREATE TABLE KB_SUBE(
    ID NUMBER(19) GENERATED BY DEFAULT ON NULL AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    SUBEADI VARCHAR2(50) NOT NULL,
    SUBEKODU VARCHAR2(20)
        DEFAULT ON NULL
        ('S' || TO_CHAR(KB_SUBEKODU_SEQ.NEXTVAL, 'FM0000')) UNIQUE,
    SUBETELEFONNO VARCHAR2(11) NOT NULL,
    SUBEADRES VARCHAR2(50) NOT NULL,
    SUBEDURUMKODU NUMBER(1) CHECK (SUBEDURUMKODU IN (1, 2)) NOT NULL,
    RECORDUSER VARCHAR2(10),
    RECORDDATE DATE DEFAULT SYSDATE
);
drop table KB_SUBE;
drop table KB_PERSONEL;
CREATE TABLE KB_PERSONEL (
    ID NUMBER(19) GENERATED BY DEFAULT ON NULL AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    AD VARCHAR2(50) NOT NULL,
    SOYAD VARCHAR2(50) NOT NULL,
    ROL VARCHAR2(20) NOT NULL,
    SICIL VARCHAR2(20) 
        DEFAULT ON NULL
        ('KB' || TO_CHAR(KB_SICIL_SEQ.NEXTVAL, 'FM0000')) UNIQUE,
    SIFRE VARCHAR2(50) NOT NULL,
    TCKN NUMBER(11) UNIQUE NOT NULL,
    TELEFONNO VARCHAR2(13) NOT NULL,
    ADRES VARCHAR2(50) NOT NULL,
    EPOSTA VARCHAR2(25) NOT NULL,
    SUBESUBEKODU VARCHAR2(20) NOT NULL,
    CONSTRAINT FK_SUBE FOREIGN KEY (SUBESUBEKODU) REFERENCES KB_SUBE(SUBEKODU) ON DELETE CASCADE,
    DURUMKODU NUMBER(1) CHECK (DURUMKODU IN (1, 2)) NOT NULL,
    RECORDUSER VARCHAR2(10),
    RECORDDATE DATE DEFAULT SYSDATE
);
drop table KB_PERSONEL;

alter table KB_PERSONEL modify ID NUMBER(19) DEFAULT ON NULL (KB_PERSONEL_SEQ.NEXTVAL);
alter table KB_SUBE modify ID NUMBER(19);

/* KB_PERSONEL tablosundaki SICIL alanı 'KB_' ile başlamalıdır. */
/* KB'den sonra 4 haneli rakam gelmelidir. Örn: KB1234 */
