-- KB_HESAPBILGILERI için sıfırdan 100 ayrı INSERT
-- HESAPNO: 1-4 şube | 5-12 HESAPACILISTARIHI (DDMMYYYY) | 13-16 sequence
-- IBAN: TR + 2 kontrol hanesi + 00069 + 0 + 16 haneli HESAPNO

DECLARE
    V_ID             NUMBER(19);
    V_MUSTERI_ID     NUMBER(19);
    V_SUBE_KODU      VARCHAR2(20);
    V_ACILIS_TARIHI  DATE;
    V_HESAP_NO       VARCHAR2(16);
    V_BBAN           VARCHAR2(22);
    V_IBAN           VARCHAR2(26);
    V_KONTROL        NUMBER;
    V_ADET           NUMBER;
BEGIN
    -- 1. INSERT: musteri001@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri001@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 6375.45, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0001', V_ACILIS_TARIHI);

    -- 2. INSERT: musteri002@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri002@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 7750.90, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0002', V_ACILIS_TARIHI);

    -- 3. INSERT: musteri003@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri003@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 9126.35, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0003', V_ACILIS_TARIHI);

    -- 4. INSERT: musteri004@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri004@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 10501.80, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0004', V_ACILIS_TARIHI);

    -- 5. INSERT: musteri005@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri005@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 11877.25, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0005', V_ACILIS_TARIHI);

    -- 6. INSERT: musteri006@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri006@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 13252.70, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0006', V_ACILIS_TARIHI);

    -- 7. INSERT: musteri007@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri007@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 14628.15, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0007', V_ACILIS_TARIHI);

    -- 8. INSERT: musteri008@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri008@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 16003.60, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0008', V_ACILIS_TARIHI);

    -- 9. INSERT: musteri009@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri009@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 17379.05, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0009', V_ACILIS_TARIHI);

    -- 10. INSERT: musteri010@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri010@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Yatırım Hesabım', V_HESAP_NO, V_IBAN, 18754.50, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 3, 'KB0010', V_ACILIS_TARIHI);

    -- 11. INSERT: musteri011@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri011@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 20129.95, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0011', V_ACILIS_TARIHI);

    -- 12. INSERT: musteri012@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri012@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 21505.40, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0012', V_ACILIS_TARIHI);

    -- 13. INSERT: musteri013@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri013@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 22880.85, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0013', V_ACILIS_TARIHI);

    -- 14. INSERT: musteri014@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri014@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 24256.30, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0014', V_ACILIS_TARIHI);

    -- 15. INSERT: musteri015@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri015@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 25631.75, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0015', V_ACILIS_TARIHI);

    -- 16. INSERT: musteri016@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri016@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 27007.20, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0016', V_ACILIS_TARIHI);

    -- 17. INSERT: musteri017@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri017@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 28382.65, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0017', V_ACILIS_TARIHI);

    -- 18. INSERT: musteri018@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri018@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 29758.10, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0018', V_ACILIS_TARIHI);

    -- 19. INSERT: musteri019@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri019@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 31133.55, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0019', V_ACILIS_TARIHI);

    -- 20. INSERT: musteri020@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri020@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Yatırım Hesabım', V_HESAP_NO, V_IBAN, 32509.00, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 3, 'KB0020', V_ACILIS_TARIHI);

    -- 21. INSERT: musteri021@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri021@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 33884.45, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0021', V_ACILIS_TARIHI);

    -- 22. INSERT: musteri022@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri022@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 35259.90, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0022', V_ACILIS_TARIHI);

    -- 23. INSERT: musteri023@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri023@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 36635.35, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0023', V_ACILIS_TARIHI);

    -- 24. INSERT: musteri024@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri024@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 38010.80, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0024', V_ACILIS_TARIHI);

    -- 25. INSERT: musteri025@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri025@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 39386.25, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0025', V_ACILIS_TARIHI);

    -- 26. INSERT: musteri026@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri026@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 40761.70, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0026', V_ACILIS_TARIHI);

    -- 27. INSERT: musteri027@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri027@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 42137.15, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0027', V_ACILIS_TARIHI);

    -- 28. INSERT: musteri028@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri028@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 43512.60, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0028', V_ACILIS_TARIHI);

    -- 29. INSERT: musteri029@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri029@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 44888.05, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0029', V_ACILIS_TARIHI);

    -- 30. INSERT: musteri030@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri030@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Yatırım Hesabım', V_HESAP_NO, V_IBAN, 46263.50, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 3, 'KB0030', V_ACILIS_TARIHI);

    -- 31. INSERT: musteri031@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri031@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 47638.95, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0031', V_ACILIS_TARIHI);

    -- 32. INSERT: musteri032@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri032@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 49014.40, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0032', V_ACILIS_TARIHI);

    -- 33. INSERT: musteri033@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri033@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 50389.85, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0033', V_ACILIS_TARIHI);

    -- 34. INSERT: musteri034@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri034@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 51765.30, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0034', V_ACILIS_TARIHI);

    -- 35. INSERT: musteri035@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri035@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 53140.75, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0035', V_ACILIS_TARIHI);

    -- 36. INSERT: musteri036@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri036@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 54516.20, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 2, 'KB0036', V_ACILIS_TARIHI);

    -- 37. INSERT: musteri037@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri037@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 55891.65, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0037', V_ACILIS_TARIHI);

    -- 38. INSERT: musteri038@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri038@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 57267.10, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0038', V_ACILIS_TARIHI);

    -- 39. INSERT: musteri039@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri039@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 58642.55, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0039', V_ACILIS_TARIHI);

    -- 40. INSERT: musteri040@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri040@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Yatırım Hesabım', V_HESAP_NO, V_IBAN, 60018.00, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 3, 'KB0040', V_ACILIS_TARIHI);

    -- 41. INSERT: musteri041@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri041@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 61393.45, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0041', V_ACILIS_TARIHI);

    -- 42. INSERT: musteri042@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri042@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 62768.90, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0042', V_ACILIS_TARIHI);

    -- 43. INSERT: musteri043@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri043@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 64144.35, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0043', V_ACILIS_TARIHI);

    -- 44. INSERT: musteri044@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri044@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 65519.80, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0044', V_ACILIS_TARIHI);

    -- 45. INSERT: musteri045@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri045@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 66895.25, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0045', V_ACILIS_TARIHI);

    -- 46. INSERT: musteri046@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri046@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 68270.70, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0046', V_ACILIS_TARIHI);

    -- 47. INSERT: musteri047@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri047@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 69646.15, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0047', V_ACILIS_TARIHI);

    -- 48. INSERT: musteri048@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri048@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 71021.60, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0048', V_ACILIS_TARIHI);

    -- 49. INSERT: musteri049@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri049@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 72397.05, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0049', V_ACILIS_TARIHI);

    -- 50. INSERT: musteri050@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri050@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Yatırım Hesabım', V_HESAP_NO, V_IBAN, 73772.50, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 3, 'KB0050', V_ACILIS_TARIHI);

    -- 51. INSERT: musteri051@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri051@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 75147.95, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0051', V_ACILIS_TARIHI);

    -- 52. INSERT: musteri052@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri052@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 76523.40, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0052', V_ACILIS_TARIHI);

    -- 53. INSERT: musteri053@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri053@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 77898.85, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0053', V_ACILIS_TARIHI);

    -- 54. INSERT: musteri054@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri054@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 79274.30, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0054', V_ACILIS_TARIHI);

    -- 55. INSERT: musteri055@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri055@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 80649.75, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0055', V_ACILIS_TARIHI);

    -- 56. INSERT: musteri056@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri056@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 82025.20, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0056', V_ACILIS_TARIHI);

    -- 57. INSERT: musteri057@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri057@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 83400.65, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0057', V_ACILIS_TARIHI);

    -- 58. INSERT: musteri058@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri058@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 84776.10, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0058', V_ACILIS_TARIHI);

    -- 59. INSERT: musteri059@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri059@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 86151.55, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0059', V_ACILIS_TARIHI);

    -- 60. INSERT: musteri060@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri060@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Yatırım Hesabım', V_HESAP_NO, V_IBAN, 87527.00, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 3, 'KB0060', V_ACILIS_TARIHI);

    -- 61. INSERT: musteri061@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri061@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 88902.45, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0061', V_ACILIS_TARIHI);

    -- 62. INSERT: musteri062@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri062@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 90277.90, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0062', V_ACILIS_TARIHI);

    -- 63. INSERT: musteri063@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri063@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 91653.35, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0063', V_ACILIS_TARIHI);

    -- 64. INSERT: musteri064@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri064@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 93028.80, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0064', V_ACILIS_TARIHI);

    -- 65. INSERT: musteri065@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri065@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 94404.25, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0065', V_ACILIS_TARIHI);

    -- 66. INSERT: musteri066@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri066@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 95779.70, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0066', V_ACILIS_TARIHI);

    -- 67. INSERT: musteri067@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri067@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 97155.15, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0067', V_ACILIS_TARIHI);

    -- 68. INSERT: musteri068@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri068@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 98530.60, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 2, 'KB0068', V_ACILIS_TARIHI);

    -- 69. INSERT: musteri069@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri069@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 99906.05, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0069', V_ACILIS_TARIHI);

    -- 70. INSERT: musteri070@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri070@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Yatırım Hesabım', V_HESAP_NO, V_IBAN, 101281.50, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 3, 'KB0070', V_ACILIS_TARIHI);

    -- 71. INSERT: musteri071@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri071@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 102656.95, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0071', V_ACILIS_TARIHI);

    -- 72. INSERT: musteri072@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri072@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Birikim Hesabım', V_HESAP_NO, V_IBAN, 104032.40, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 2, 'KB0072', V_ACILIS_TARIHI);

    -- 73. INSERT: musteri073@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri073@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 105407.85, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0073', V_ACILIS_TARIHI);

    -- 74. INSERT: musteri074@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri074@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 106783.30, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0074', V_ACILIS_TARIHI);

    -- 75. INSERT: musteri075@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'musteri075@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ana Hesabım', V_HESAP_NO, V_IBAN, 108158.75, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0075', V_ACILIS_TARIHI);

    -- 76. INSERT: kurumsal001@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal001@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 109534.20, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0076', V_ACILIS_TARIHI);

    -- 77. INSERT: kurumsal002@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal002@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 110909.65, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0077', V_ACILIS_TARIHI);

    -- 78. INSERT: kurumsal003@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal003@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 112285.10, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0078', V_ACILIS_TARIHI);

    -- 79. INSERT: kurumsal004@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal004@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 113660.55, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0079', V_ACILIS_TARIHI);

    -- 80. INSERT: kurumsal005@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal005@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 115036.00, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0080', V_ACILIS_TARIHI);

    -- 81. INSERT: kurumsal006@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal006@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 116411.45, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0081', V_ACILIS_TARIHI);

    -- 82. INSERT: kurumsal007@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal007@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 117786.90, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0082', V_ACILIS_TARIHI);

    -- 83. INSERT: kurumsal008@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal008@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 119162.35, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0083', V_ACILIS_TARIHI);

    -- 84. INSERT: kurumsal009@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal009@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 120537.80, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0084', V_ACILIS_TARIHI);

    -- 85. INSERT: kurumsal010@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal010@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 121913.25, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0085', V_ACILIS_TARIHI);

    -- 86. INSERT: kurumsal011@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal011@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 123288.70, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0086', V_ACILIS_TARIHI);

    -- 87. INSERT: kurumsal012@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal012@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 124664.15, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0087', V_ACILIS_TARIHI);

    -- 88. INSERT: kurumsal013@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal013@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 126039.60, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0088', V_ACILIS_TARIHI);

    -- 89. INSERT: kurumsal014@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal014@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 127415.05, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0089', V_ACILIS_TARIHI);

    -- 90. INSERT: kurumsal015@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal015@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 128790.50, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0090', V_ACILIS_TARIHI);

    -- 91. INSERT: kurumsal016@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal016@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 130165.95, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0091', V_ACILIS_TARIHI);

    -- 92. INSERT: kurumsal017@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal017@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 131541.40, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0092', V_ACILIS_TARIHI);

    -- 93. INSERT: kurumsal018@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal018@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 132916.85, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0093', V_ACILIS_TARIHI);

    -- 94. INSERT: kurumsal019@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal019@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 134292.30, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0094', V_ACILIS_TARIHI);

    -- 95. INSERT: kurumsal020@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal020@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 135667.75, V_SUBE_KODU, 2, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0095', V_ACILIS_TARIHI);

    -- 96. INSERT: kurumsal021@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal021@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 137043.20, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0096', V_ACILIS_TARIHI);

    -- 97. INSERT: kurumsal022@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal022@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 138418.65, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0097', V_ACILIS_TARIHI);

    -- 98. INSERT: kurumsal023@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal023@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 139794.10, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0098', V_ACILIS_TARIHI);

    -- 99. INSERT: kurumsal024@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal024@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 141169.55, V_SUBE_KODU, 1, V_ACILIS_TARIHI, 2, V_MUSTERI_ID, 1, 'KB0099', V_ACILIS_TARIHI);

    -- 100. INSERT: kurumsal025@test.com
    SELECT ID, SUBESUBEKODU INTO V_MUSTERI_ID, V_SUBE_KODU
    FROM KB_MUSTERIBILGILERI WHERE EPOSTA = 'kurumsal025@test.com';

    V_ACILIS_TARIHI := SYSDATE;
    LOOP
        SELECT KB_HESAPBILGILERI_SEQ.NEXTVAL INTO V_ID FROM DUAL;
        V_HESAP_NO := LPAD(REGEXP_SUBSTR(V_SUBE_KODU, '[0-9]+$'), 4, '0')
                      || TO_CHAR(V_ACILIS_TARIHI, 'DDMMYYYY')
                      || LPAD(MOD(V_ID, 10000), 4, '0');
        SELECT COUNT(*) INTO V_ADET FROM KB_HESAPBILGILERI WHERE HESAPNO = V_HESAP_NO;
        EXIT WHEN V_ADET = 0;
    END LOOP;

    V_BBAN := '00069' || '0' || V_HESAP_NO;
    V_KONTROL := 98 - MOD(TO_NUMBER(V_BBAN || '292700'), 97);
    V_IBAN := 'TR' || LPAD(V_KONTROL, 2, '0') || V_BBAN;

    INSERT INTO KB_HESAPBILGILERI (ID, HESAPADI, HESAPNO, IBAN, BAKIYE, SUBESUBEKODU, DOVIZCINSI, HESAPACILISTARIHI, HESAPDURUMKODU, MUSTERIBILGILERIID, HESAPTIPI, RECORDUSER, RECORDDATE)
    VALUES (V_ID, 'Ticari Hesap', V_HESAP_NO, V_IBAN, 142545.00, V_SUBE_KODU, 3, V_ACILIS_TARIHI, 1, V_MUSTERI_ID, 1, 'KB0100', V_ACILIS_TARIHI);

    COMMIT;
END;
/

-- Kontrol: TARIH_HANELERI ile HESAP_ACILIS_TARIHI aynı görünmelidir.
SELECT
    H.ID,
    M.EPOSTA,
    H.SUBESUBEKODU,
    H.HESAPNO,
    SUBSTR(H.HESAPNO, 1, 4) AS SUBE_HANELERI,
    SUBSTR(H.HESAPNO, 5, 8) AS TARIH_HANELERI,
    TO_CHAR(H.HESAPACILISTARIHI, 'DDMMYYYY') AS HESAP_ACILIS_TARIHI,
    SUBSTR(H.HESAPNO, 13, 4) AS SEQUENCE_HANELERI,
    H.IBAN,
    LENGTH(H.HESAPNO) AS HESAPNO_UZUNLUK,
    LENGTH(H.IBAN) AS IBAN_UZUNLUK
FROM KB_HESAPBILGILERI H
JOIN KB_MUSTERIBILGILERI M ON M.ID = H.MUSTERIBILGILERIID
WHERE M.EPOSTA LIKE '%@test.com'
ORDER BY H.ID;

